name: Dev Push

on:
  push:
    branches:
      - dev

jobs:
  dev-build:
    uses: cosmicmind/github-actions/.github/workflows/dev-build.yml@release
  test:
    needs: dev-build
    uses: cosmicmind/github-actions/.github/workflows/test.yml@release